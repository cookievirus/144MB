/* version.h - one place the build says what it is.

   The number was in four: a comment in main.c, a comment in the Makefile, a
   comment in build.bat and the README's heading. Three of those are comments
   and cannot be wrong in a way that matters; the fourth is now on screen, in
   the corner of every frame, where being stale is a submission that claims to
   be a version it is not. */
#ifndef VERSION_H
#define VERSION_H

#define GAME_VERSION "1.9.3"

#endif /* VERSION_H */
